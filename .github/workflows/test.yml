name: test

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name'
        required: true

jobs:
  Test:
    outputs:
      repo_info: ${{steps.set-repo_info.outputs.repo_info}}
    runs-on: ubuntu-latest
    steps:
    # マスクテスト
    - name: test
      run: |
        # echo "::add-mask::${{ github.event.inputs.name }}"
        echo "Access ""${{ github.event.inputs.name }}"
        # 入力値を参照
        # curl ${{ github.event.inputs.name }}
        # シークレット情報を参照
        # curl ${{secrets.SECRET_URL}}
        # echo "::notice::""INPUT INFO:"
        # echo "::notice::""  PROJECT:XXXXX"
        # echo "::notice::""  COLLECTION:XXXXX"
        # echo "::notice::""  REPOSITORY(BRANCH OR TAG):XXXXX"
        # echo "::notice::""SEARCH RESULT:"
        # echo "::notice::""  JIRA TICKET NO DATA"
        echo "aaaaa" >> $GITHUB_STEP_SUMMARY
        echo "bbbbb" >> $GITHUB_STEP_SUMMARY
        echo ${{ github.event.inputs.name }}  >> $GITHUB_STEP_SUMMARY
